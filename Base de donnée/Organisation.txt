Table user :
1) username
2) password
3) version (essai|free|premium) // a penser pour free essai
4) date d'expiration (if essai -> free) | (if free -> free) | (if premium -> free) // a checker a chaque conn
5) nom 
6) prenom
7) Image ? // facultatif
8) programssub // identifiants des programmes rejoinds
9) programscreated // identifiants des programmes créés

Table programmes :
1) id // pour le qrcode | généré aléatoirement exemple : 834732185470
2) auteur // par username
3)


// a finir